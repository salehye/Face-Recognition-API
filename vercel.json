{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "api/**/*.py": {
      "excludeFiles": "{test_images/**,test_client.py,venv/**,__pycache__/**,.env.example,docker-compose.yml,Dockerfile,.dockerignore,.gitattributes,.gitignore,*.md}"
    }
  }
}

// NOTE: هذا المشروع غير متوافق فعليًا مع Vercel بسبب:
// - نموذج buffalo_l (~330MB) يتجاوز حد 250MB
// - مكتبات النظام (libgl1, libglib2.0-0, إلخ) غير مدعومة
// - استخدم Railway.app أو Fly.io أو Render.com بدلاً من ذلك